{
  "framework": "vite",
  "buildCommand": "cd client && pnpm install && pnpm run build",
  "outputDirectory": "client/dist",
  "cleanUrls": true,
  "ignoreCommand": "echo 'Skipping server folder'"
}
